# Memory Policy

Short-term state stays in the active thread. Long-term memory is partitioned into semantic, episodic, procedural, project, and user memory.

Shared memory is read-only by default. Long-term memory writes require evidence and a gate decision.
