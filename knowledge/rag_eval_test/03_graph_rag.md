# RAG Eval: GraphRAG

Document id: rag_eval_graph_rag.

GraphRAG extracts entities and relationships from documents, builds a knowledge graph, and retrieves relevant subgraphs or community summaries. It is useful when answers depend on relationships among concepts rather than isolated text snippets.

