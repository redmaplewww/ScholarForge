# RAG Eval: Memory Gate Policy

Document id: rag_eval_memory_gate_policy.

Long-term memory writes require supporting evidence and an approval gate. The memory system should create a proposal before persistent memory is changed. Shared memory partitions are read-only unless a human explicitly approves the update policy.

