# RAG Eval: BM25 Keyword Retrieval

Document id: rag_eval_bm25_keyword.

BM25 is a lexical keyword retrieval method. It ranks documents with term frequency, inverse document frequency, and document length normalization. BM25 does not need embeddings and works best when the query shares exact terms with the target document.

