# RAG Eval: Cross Encoder Reranker

Document id: rag_eval_cross_encoder_reranker.

A cross-encoder reranker scores each query and candidate document pair after the first-stage retriever. Reranking improves precision by using deeper interaction between the query and document text, but it is slower than the initial retriever.

