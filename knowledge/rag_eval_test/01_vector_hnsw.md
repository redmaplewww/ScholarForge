# RAG Eval: HNSW Vector Index

Document id: rag_eval_vector_hnsw.

HNSW is an approximate nearest neighbor index for vector search. It stores embeddings in layered graph links. The ef_search parameter controls the recall and latency tradeoff during query time. A larger ef_search usually improves retrieval recall but increases search cost.

